server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
     server:
       git:
         uri:
         default-label:
         username:
         password:
         clone-on-start: